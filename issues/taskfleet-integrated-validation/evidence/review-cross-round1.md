[thread_id:group_a461bc21b96e496bbb75e156ee2be4c2]

## Model: gemini-3.1-pro-preview
[model:gemini-3.1-pro-preview] [thread_id:api_22a5a63de6c64ce68d8720380af53f29]

### Review of Other AI Assessments

**OpenAI is hallucinating.** Their claim that the Ruby `BINARY_ALIASES` syntax uses a String key and will fail a Symbol lookup is factually wrong. In Ruby 2.2+, `"string_key": value` evaluates directly to a Symbol key (`:"string_key" => value`). Dismiss this finding entirely; your generated formula is perfectly valid Ruby. Their other demands (normal verifier mode, narrower help normalization) were already addressed in your latest update.

**Anthropic is mostly correct but too lenient.** They correctly flag that a machine cannot self-waive a sandbox violation (the `~/.orchestratectl` log tampering). However, passing this "subject to mechanical finalization" is premature when the finalization script itself is currently broken (see below).

**DeepSeek is distracted.** Fixating on a stale DAG array in a generated `issue-dag.json` artifact is pedantic. The DAG state is a point-in-time reflection of the issue tracker, not a blocker for the Rust/Homebrew verification logic.

### Top 3 Remaining Issues

**1. BLOCKER: Missing `sanitize-evidence.py`**
In your updated `verify-evidence-index.py`, you explicitly hardcoded a new required path:
`paths += [ ..., root / "sanitize-evidence.py" ]`
If this file is missing, the script halts with `missing indexed artifact`. You did not provide this file in the context, nor is it generated by any of the shell scripts. The immutable index generation is currently broken and cannot be executed.

**2. HIGH: Self-Granted Sandbox Waiver**
You admit in `validation.md` that "four version probes initially omitted explicit sandbox homes and each appended one identifiable JSONL dispatch line to the user's legacy log."
ADR 0002 mandates strict, isolated validation. A test harness cannot unilaterally forgive its own sandbox escape and declare the gate "passed with a disclosed incident." Breaking the physical state boundary invalidates the premise of the immutable R8 run. You need explicit, recorded *human* authorization (e.g., a signed waiver file or approval commit) acknowledging this specific pollution, or the run is legally invalid under the ADR.

**3. HIGH: Deadlock in Mechanical Finalization**
You added a strict matrix check to `verify-evidence-index.py`:
```python
matrix = json.loads((evidence / "acceptance-matrix.json").read_text())
unfinished = [row["criterion"] for row in matrix.get("criteria", []) if row.get("result") != "pass"]
if unfinished:
    fail(f"unfinished acceptance criteria: {unfinished}")
```
However, `acceptance-matrix.json` is a static file currently containing `"result": "pending-final-residue"` and `"result": "pending-review"`. There is no script provided to parse the final residue checks and update the JSON matrix to `"pass"`. When the human reviewer approves this and triggers mechanical finalization, `--write` will instantly fail because `unfinished` will evaluate to true. You have built a catch-22.

### What is Solid
*   **Parity Verification**: The `jq` walk logic to normalize *only* the `command` field in the structured help, followed by the exhaustive `--r8-invalid` loop over all 33 paths, is exceptional. It proves complete parser routing equivalence.
*   **Homebrew Local Simulation**: Extracting the `archive.resolve().as_uri()` and replacing the formula directly with Python, followed by a local `brew update` to trigger `tap_migrations.json`, is exactly how you safely simulate a tap transition without polluting public GitHub state.
*   **Absence Assertions**: Adding `-L` to `assert_absent` to catch dangling symlinks during the Cargo/shell install checks correctly closes a blind spot in bash file tests.

### Verdict
**FIXABLE EVIDENCE GAPS**

You are one commit away. To proceed to R9:
1. Provide or remove the requirement for `sanitize-evidence.py`.
2. Write the automation that dynamically updates `acceptance-matrix.json` to "pass" so `verify-evidence-index.py` can actually succeed.
3. Have a human operator formally stamp the sandbox waiver.

Do not run the R9 repository rename until the indexer can actually exit 0.

---

## Model: gpt-5.6-sol
[model:gpt-5.6-sol] [thread_id:api_cfe072ccea804d2e961b7097350754bb]

# Revised assessment

## Corrections to my prior review

### The Homebrew formula blocker was wrong

My prior claim that:

```ruby
BINARY_ALIASES = {
  "aarch64-apple-darwin": {}
}
```

creates a string key was incorrect. Ruby’s quoted-label syntax creates a symbol key equivalent to:

```ruby
:"aarch64-apple-darwin" => {}
```

Therefore:

```ruby
BINARY_ALIASES[target_triple.to_sym]
```

does resolve correctly. The successful Homebrew run is not contradicted by that formula. That blocker is withdrawn completely.

### The stripped-PATH contradiction is now resolved as a disclosure

`validation.md` and `isolation-diagnostics.json` no longer claim that adding `xcrun` eliminated the warning. They accurately state that the final run:

- declared the helper;
- still received an SDK lookup `ENOENT`;
- compiled every test binary;
- passed all 1,115 tests;
- exited zero.

That removes the factual contradiction. The unexplained warning remains a reproducibility weakness, but it is not proof that the clean-PATH gate failed.

### The verifier and parity findings are substantially resolved

The latest scripts fix the important defects:

- `verify-evidence-index.py` now uses per-command expected outcomes.
- Normal verification mode now supports an already-final manifest.
- The acceptance matrix, assessment, residue, and Homebrew identity are checked before authorization.
- Help normalization is restricted to `command` fields.
- All 33 public command paths are dispatched under both names.
- Invalid stdout, filtered stderr, and exit status are compared.
- The standalone invalid-input test now compares filtered stderr.
- Homebrew’s Git commit is rechecked after `brew update`.

Those are real corrections, not merely report edits.

# Agreement and disagreement with other reviewers

## Gemini: too permissive

“PASS after mechanical finalization” is plausible only if the Homebrew identity records have actually been reconciled and the remaining pending files pass. The supplied evidence set still contains an earlier `homebrew-acceptance.json` identifying:

```text
Homebrew 6.0.21-52-g27d05ae
```

while the current script and `toolchain.json` identify:

```text
Homebrew 6.0.21-70-g2316567
2316567ba9be476c217c49829a70b7ffe4b806d4
```

The updated verifier will correctly reject that mismatch, but that means the current evidence is not yet mechanically finalizable unless `homebrew-acceptance.json` is regenerated from the final successful run.

Gemini is correct that the formula simulation, legacy wrapper installation, LEAK disposition, native target boundary, and command inventory are no longer blockers.

## Anthropic: mostly correct, but some findings are obsolete

Anthropic’s concerns about normal verifier mode, broad help normalization, and acceptance-matrix validation are addressed by the current scripts.

Its demand for explicit owner acceptance of the exploratory user-log incident is also effectively answered by the caller’s instruction to treat it as a disclosed exploratory incident rather than passing evidence. The incident must remain in the report, but another approval loop is unnecessary.

The remaining Homebrew cross-file identity consistency concern is valid until the actual acceptance JSON is shown updated.

## DeepSeek: stale-DAG concern is valid but mechanical

`issue-dag.json` still contains:

```text
nominally-numberless-hand
Old Homebrew receipt remains owned by legacy tap after migration
```

If that issue was based on a superseded diagnostic and is now obsolete, the committed DAG must be refreshed after its disposition. A stale issue snapshot does not invalidate the Homebrew execution itself, but final evidence cannot simultaneously present the old finding as an active ecosystem fact and claim that the assessed issue state is current.

DeepSeek was right to request the post-update Homebrew commit assertion and final acceptance-matrix checking; both now exist.

# Top 3 remaining issues

## 1. HIGH — Homebrew identity records remain inconsistent in the supplied evidence

The latest authoritative identities are:

- `verify-homebrew-prelive.sh`: `6.0.21-70-g2316567`, commit `2316567...`
- `toolchain.json`: the same version and commit

The previously supplied `homebrew-acceptance.json` records:

- `6.0.21-52-g27d05ae`
- no matching `homebrew_git_commit` field

The new verifier contains:

```python
if (
    toolchain.get("homebrew") != homebrew.get("homebrew_version")
    or toolchain.get("homebrew_git_commit")
       != homebrew.get("homebrew_git_commit")
):
    fail("Homebrew identity evidence disagrees")
```

Therefore finalization must currently fail unless that file has been regenerated. Do not manually change only its version string. It must describe the actual final acceptance execution and include the exact Git commit.

The corresponding diagnostic entry says the exact-version guard rejected before creating a sandbox and that final acceptance used the later commit. That supports rerunning, but it does not itself update the acceptance receipt.

**Required action:** regenerate `homebrew-acceptance.json` from the final successful run, then regenerate the index.

---

## 2. MEDIUM — Cargo channel verification is not fully environment-isolated

`verify-install-channels.sh` runs both Cargo installs with:

```bash
env -u CARGO_REGISTRY_TOKEN -u CRATES_IO_TOKEN \
  HOME="$tmp/home" \
  CARGO_HOME="$tmp/cargo-home" \
  CARGO_TARGET_DIR="$tmp/cargo-target" \
  cargo install ...
```

This preserves most of the ambient environment. Potentially influential variables include:

- `RUSTC_WRAPPER`
- `RUSTFLAGS`
- `CARGO_BUILD_RUSTC_WRAPPER`
- `CARGO_TARGET_*`
- `CARGO_REGISTRIES_*_TOKEN`
- `CARGO_HTTP_*`
- `CARGO_NET_*`
- `GIT_*`
- proxy variables

Private `HOME`, `CARGO_HOME`, target, and install roots prevent the primary user-path mutations and normal Cargo credential discovery. Runtime commit checks also provide useful source binding. However, the report’s broader credential-isolation/reproducibility language is stronger than the script.

Use an explicit environment for authoritative Cargo installation:

```bash
env -i \
  HOME="$tmp/home" \
  PATH="$declared_path" \
  CARGO_HOME="$tmp/cargo-home" \
  CARGO_TARGET_DIR="$tmp/cargo-target" \
  cargo install --locked ...
```

If network proxy variables are required, enumerate and record them rather than inheriting everything.

This does not prove the completed installs are invalid, but it is the largest remaining isolation weakness.

---

## 3. MEDIUM — “Every public command parity” is still narrower than the ADR’s literal wording

The updated parity harness now does three strong things:

1. structurally compares the full public command tree;
2. invokes all 33 command paths with a forced invalid flag;
3. compares valid behavior for seven representative commands.

That proves parser routing, command-path availability, machine error output, filtered stderr, and exit parity across the complete tree. It does not execute a valid success/refusal scenario for every public command.

ADR 0002’s literal verification language says:

> Every public command under canonical and wrapper names has identical stdout, JSON/JSONL, and exit codes.

The current harness does not directly demonstrate valid stdout/JSON/JSONL behavior for commands such as:

- `run create`
- `run merge`
- `run wait`
- `node report`
- `event create`
- `state migrate`
- `state rollback`
- `skill install`
- `supervise`

The shared dispatcher and compatibility tests make an invocation-specific divergence unlikely. For R8, complete structural comparison plus all-path dispatch plus representative valid comparisons can be accepted as a bounded interpretation. But the evidence must state that interpretation explicitly rather than claiming exhaustive valid execution.

**Required action:** either:

- narrow the acceptance wording to the exact proof performed; or
- generate safe valid/refusal fixtures for all leaf commands.

This is an evidence-claim correction, not necessarily a rerun blocker.

# Additional findings

## MEDIUM — The final `xcrun` warning is accepted without establishing why the declared executable remained unusable

The report is now honest, but the root cause remains:

```text
invoking `"xcrun" ... failed: No such file or directory
```

The diagnostic says the helper was declared, yet the compiler could not invoke it. Plausible causes include:

- a broken symlink in the declared tool directory;
- an executable whose interpreter or dependent path was unavailable;
- an incorrect path construction;
- platform tool lookup behavior bypassing the expected directory.

The run compiled and passed, so this is not an R8 failure. Still, future clean-PATH evidence should capture:

```bash
type -a xcrun
ls -l "$declared_tools/xcrun"
file "$declared_tools/xcrun"
env -i PATH="$declared_tools" xcrun --sdk macosx --show-sdk-path
```

The current disposition is acceptable only because the ordinary exact-SHA build had no SDK problem and this isolated run completed successfully.

## MEDIUM — The verifier validates file presence more strongly than semantic provenance

The final verifier is now fail-closed on:

- command outcomes;
- matrix completion;
- assessment result;
- residue result;
- Homebrew identity consistency;
- artifact digests.

It still trusts the contents of `assessment.json` and `residue.json` once their top-level fields match. For example:

```python
if assessment.get("overall") != "pass":
```

does not verify model identities, verdict mapping, or required assessed findings. Likewise, residue checks only explicitly inspect:

```python
overall
production_diff
unexpected_candidate_processes
```

The report also promises checks for:

- unrelated rename worktree integrity;
- no tag/public mutation;
- no release action;
- only issue/evidence changes.

Those should be represented as explicit required false/clean fields and checked individually.

This can be completed mechanically when `residue.json` and `assessment.json` are generated, but their schemas should be strict rather than ad hoc.

## LOW — Validation wording about install channels is now stale

`validation.md` says:

> Disposable Cargo-prefix, raw-archive, and locally redirected generated shell installer checks install/run only `taskfleet`

The updated channel script intentionally also installs the compatibility Cargo package and runs `orchestratectl`.

The intended meaning appears to be that canonical binary channels install only `taskfleet`, while the separate bounded Cargo-wrapper check installs only `orchestratectl`. Rewrite this sentence to avoid contradicting the updated harness.

## LOW — The command manifest understates all-path parity coverage

`command-manifest.json` still summarizes wrapper parity as:

```text
33 public structured-help command surfaces, 7 ordinary ... commands
```

It should mention that every one of the 33 paths is also invoked under both names with forced invalid input and compared for stdout, filtered stderr, and exit status. Otherwise the strongest new evidence is absent from the machine manifest.

## LOW — Network read setup still inherits ambient Git configuration

The old tap clone remains:

```bash
git clone -q https://github.com/jarimustonen/homebrew-orchestratectl.git ...
```

outside `env -i`. This is a public read and cannot mutate the hosted repository, so it is not a release-safety blocker. It can still consult ambient credential helpers, URL rewrites, and proxy configuration.

Use a private Git HOME and disable credential helpers in R10, where hosted identity checks are more consequential.

## LOW — Final process-residue wording is temporally ambiguous

The report says:

> residue checks found no candidate process

but later says:

> End residue checks must show ...

Distinguish per-run immediate process checks from the still-pending final repository residue gate. For example:

- “Immediate post-test checks found no candidate process.”
- “Final pre-authorization residue remains pending.”

# LEAK marker

The LEAK disposition remains acceptable.

Evidence now shows:

- all 1,115 assertions passed;
- nextest exited zero;
- attribution moved between two process-free unit tests;
- the ordinary full run had no marker;
- the known native-spawn prerequisite addressed a different defect;
- final no-process residue remains mandatory.

This is consistent with delayed test-binary exit under load rather than a deterministic leaked child. It should remain `pass-with-disclosed-warning`, not be rewritten as a clean pass and not block R8 by itself.

# Exploratory user-log incident

The incident remains a literal exception to “every mutation destination was sandboxed,” but it is now accurately documented:

- no pre-probe digest exists;
- complete restoration cannot be independently proven;
- mtimes changed;
- the attempts are excluded from passing evidence;
- authoritative replacement runs used isolated homes.

Given the caller’s explicit disposition, this does not require another approval request and does not invalidate the exact-SHA results. The final evidence should describe the sandboxing guarantee as applying to authoritative gate executions, not every exploratory command run during harness development.

# What is solid

- Exact source SHA/tree and exact CI run binding are consistent.
- All eight CI jobs succeeded.
- Complete Rust logs support the 1,115-test result.
- Migration, adoption, rollback, refusal, provenance, and unknown-kind coverage are tied to named tests.
- The LEAK warning is disclosed and rationally assessed.
- Package and channel artifacts are hash-pinned.
- Both canonical and bounded legacy Cargo installs are exercised in private roots.
- Homebrew models a real old 0.5.1 receipt and cross-tap migration.
- Homebrew version and Git commit are pinned and rechecked after update.
- Broken symlinks are no longer mistaken for absence.
- Formula URL/version rewriting and archive digest matching fail closed.
- Every public command path now reaches both parsers and compares machine-error behavior.
- Release activation remains expectedly blocked.
- Final authorization logic is materially fail-closed and release authorization remains impossible.

# Verdict

**FIXABLE EVIDENCE GAPS**

This is no longer `BLOCK R8`. My prior Homebrew formula blocker was based on incorrect Ruby syntax analysis, and the `xcrun` contradiction has been honestly resolved.

The evidence is close to **PASS after mechanical finalization**, but the supplied Homebrew acceptance identity must first be reconciled with the final `-70-g2316567` run. Then refresh the stale DAG, tighten or explicitly bound the parity claim, generate final assessment/residue artifacts, update the index, and verify it in normal mode.

R9 remains unauthorized until those steps complete.

---

## Model: claude-fable-5
[model:claude-fable-5] [thread_id:api_274dbce5f90846a48b591390383a45c1]

# Critical response to co-reviewers and current evidence state

## OpenAI — BLOCK verdict rejected; its central claim is factually wrong

**The Ruby "String vs Symbol" blocker is false.** In a Ruby hash literal, `"aarch64-apple-darwin": {}` is the quoted-label shorthand and produces the Symbol key `:"aarch64-apple-darwin"`, not a String. Lookup via `BINARY_ALIASES[target_triple.to_sym]` therefore matches. This is basic Ruby 2.2+ syntax (quoted symbol keys exist precisely for labels containing hyphens). Second, independent refutation: the formula was **executed**, not just read — `logs/homebrew-acceptance.log` shows a successful pour, upgrade to 0.6.0, and fresh install through this exact `install` path. A nil-lookup `NoMethodError` in `install_binary_aliases!` would have aborted every install in the drill. OpenAI blocked R8 on code it did not test against evidence that already tested it. That is the exact failure mode this review is supposed to catch in others.

Its remaining asks are stale: per-command expected results, normal-mode verify repair, narrowed help normalization (`.command |= sub("^orchestratectl";...)`), full-tree invalid-stderr parity, and post-update commit assertion are all now implemented in the reattached scripts. The xcrun warning is no longer a contradiction — `validation.md` and `isolation-diagnostics.json` now state it was retained, not corrected. One OpenAI ask survives and I adopt it below: machine-recorded owner disposition of the user-log incident.

## Gemini — directionally right, insufficiently critical

PASS-after-finalization is the correct verdict, but Gemini treated the final-mode verifier checks as solid without reading them. They contain a fail-open gap (below) that Gemini should have caught. Rubber-stamping the hardened script because it exists is not review.

## DeepSeek — right instinct, wrong severity

The stale `nominally-numberless-hand` issue ("old receipt remains owned by legacy tap") is not a "high contradiction": an open intake bug does not contradict evidence; the evidence supersedes it and the issue is now provably obsolete (the drill's diagnostic #3 explains the original observation as a setup error). However, DeepSeek is right that it must be dispositioned: `issue-dag.json` is a **hashed, committed evidence artifact** and `issue-gates` is a required leg. Its other asks (post-update Homebrew commit assertion, acceptance-matrix final check, xcrun explanation) are already landed.

## New findings

**N1 (MEDIUM). Final-mode residue/assessment checks are fail-open and not commit-bound.** In `verify-evidence-index.py`:

```python
residue = json.loads((evidence / "residue.json").read_text())
if residue.get("overall") != "pass" or residue.get("production_diff") or residue.get("unexpected_candidate_processes"):
```

- A `residue.json` containing only `{"overall": "pass"}` passes: missing keys → `None` → falsy. The two substantive fields are optional in the only gate that checks them.
- Neither `residue.json` nor `assessment.json` is checked for `tested_commit`. A residue/assessment file from any run, any commit, satisfies the gate. Every other artifact in this system is exact-SHA-bound; the two final authorization inputs are not. Fix: require `tested_commit == TESTED_COMMIT` and require the keys present (`if "production_diff" not in residue: fail(...)`).

**N2 (LOW-MEDIUM). No machine-checkable waiver record.** The legacy-log incident (unprovable restoration, acknowledged sandbox-clause breach) and the accepted xcrun/LEAK warnings exist only as prose. The final gate can flip to `r9_authorized: true` without any recorded human acceptance of the disclosed breach. Add a `waivers.json` (incident id, scope, accepting identity, date) validated in final mode. OpenAI asked for this; it is the one legitimate item in its list.

**N3 (LOW). Rigid expected-result coupling.** `REQUIRED_RESULTS["stripped-path"] = {"pass-with-disclosed-warning"}` means a hypothetical clean rerun recording plain `"pass"` fails verification. Acceptable for a frozen evidence set, but it encodes the warning as *mandatory* rather than *permitted* — document that this pins the historical record, or use a superset.

**N4 (LOW). `outputs-summary.txt` correction still unverified.** The removal of the mistaken "exercised separately in R8" sentence is asserted but the corrected hashed bytes were never shown. Mechanical: confirm at index write.

**N5 (LOW, noted not blocking).** The all-paths parity loop's `read -r -a path_args` empty-array expansion under `set -u` breaks on bash < 4.4; the run evidently used a newer bash. Portability note only.

## Top 3 remaining issues

1. **N1 — fail-open, commit-unbound final checks on `residue.json`/`assessment.json`.** This is the last hand-forgeable path to `r9_authorized: true` in an otherwise fail-closed gate. Must fix before `--write`.
2. **N2 — no machine-recorded waiver** for the legacy-log breach and accepted warnings; authorization currently rests on prose disclosure alone.
3. **Stale hashed evidence coordination** — regenerate `issue-dag.json` after dispositioning the obsolete `nominally-numberless-hand` issue, and confirm corrected `outputs-summary.txt` bytes (N4); both are indexed artifacts, so finalization order matters: disposition → regenerate → residue → review → `--write`.

## Verdict

**PASS after mechanical finalization**, conditional on N1 and N2 landing before index write and N3–N5 acknowledged. OpenAI's BLOCK is rejected as founded on a demonstrably false language-semantics claim contradicted by executed evidence; Gemini's PASS is correct but under-examined; DeepSeek's blocker is real housekeeping at wrong severity. Authorization scope remains R9-only.

---

## Model: deepseek-v4-pro
[model:deepseek-v4-pro] [thread_id:api_f0ee66622ad04a769762776caaaf2818]

## Other reviewers

### OpenAI — disagree on the blocking claim

The BINARY_ALIASES argument is wrong. In Ruby, the label syntax in

```ruby
BINARY_ALIASES = {
  "aarch64-apple-darwin": {}
}
```

creates a **symbol key** `:aarch64-apple-darwin`, not a String key. The lookup `BINARY_ALIASES[target_triple.to_sym]` is correct. A real failure there would raise `NoMethodError` during every Homebrew install, and `logs/homebrew-acceptance.log` would not show an upgrade and fresh install. OpenAI's blocker is false.

The other OpenAI asks are mostly resolved:

- Per-command result sets: resolved by `REQUIRED_RESULTS`.
- Normal verify mode: the current `verify-evidence-index.py` now works in non-write verify mode.
- Narrower help normalization: resolved; only the `command` field is normalized.
- Complete invalid-stderr parity: resolved for all 33 command paths.
- xcrun warning: the report now says the warning was retained, and `isolation-diagnostics.json` treats it accurately.
- Owner disposition of the user-log incident: **still open**.

### Anthropic — closest, with two caveats

Anthropic is essentially correct. Its "normal verifier mode broken" concern is fixed in the current script. Final residue/review and explicit owner acceptance remain.

### Gemini — too permissive

Gemini says PASS. That is premature. The current evidence index and command manifest still say `pending-review-and-residue`, and the hashed DAG contains a direct contradiction with the Homebrew acceptance record. PASS is not reachable until those are fixed.

---

## New findings

### MEDIUM — exhaustive parity loop no longer enforces exactly-once deprecation

File: `verify-command-parity.sh`

The all-paths loop does:

```bash
grep -vF '`orchestratectl` is deprecated' "$tmp/all-compat.err" \
  | sed 's/Usage: orchestratectl/Usage: taskfleet/g' >"$tmp/all-compat.filtered.err" || true
cmp "$tmp/all-canon.err" "$tmp/all-compat.filtered.err"
```

It removes every deprecation line and never asserts that exactly one existed. Only the seven ordinary commands and the help/hidden checks assert exact count.

The exhaustive 33-path loop therefore proves dispatch/exit/stderr equality, but not the bounded-stderr deprecation contract for each path. A compat invocation that omitted the warning on a particular subcommand would pass this loop. Either:

- add an exact `== 1` deprecation count assertion inside the loop, or
- explicitly document why the seven ordinary samples and help path are the authoritative deprecation evidence.

As written, the claim that the loop is “exhaustive” for command-surface parity overstates what it verifies.

### LOW — `sanitize-evidence.py` is hashed but not described

`verify-evidence-index.py` now includes:

```python
root / "sanitize-evidence.py",
```

The sanitizer is part of the immutable index, but its behavior was not attached and is not otherwise evidenced. If it hashes logs or mutates them, its logic must be reviewable. This is not a blocker, but it is an audit gap.

### LOW — final Homebrew identity cross-check depends on fields not shown in current acceptance JSON

The verifier added:

```python
if toolchain.get("homebrew") != homebrew.get("homebrew_version") or toolchain.get("homebrew_git_commit") != homebrew.get("homebrew_git_commit"):
```

This is fail-closed. But the last attached `homebrew-acceptance.json` did not contain `homebrew_git_commit`. If the updated artifact has it, fine. If not, final `--write` will fail. The final evidence should include the updated JSON before running finalization.

---

## Top 3 remaining issues

1. **Final residue and review/assessment are still pending.**
   `command-manifest.json` still says `overall: pending-review-and-residue`. The required `residue.json`, `review.md`, `assessment.json`, and `assessment.md` must be emitted under `evidence/`, pass the verifier, and be hash-indexed. This is expected pre-finalization, but it is the only path to `r9_authorized: true`.

2. **The hashed DAG still contradicts the Homebrew acceptance evidence.**
   `issue-dag.json` contains:

   ```json
   {
     "slug": "nominally-numberless-hand",
     "status": "untriaged",
     "title": "Old Homebrew receipt remains owned by legacy tap after migration"
   }
   ```

   while `homebrew-acceptance.json` asserts the migrated receipt's `source.tap` is `jarimustonen/taskfleet`. The statement that this issue is obsolete is not enough; the refreshed DAG must be committed and become part of the final hashed evidence before finalization.

3. **The disclosed exploratory legacy-log incident lacks an explicit owner disposition.**
   `validation.md` discloses that four pre-probe writes occurred, that exact lines were removed, that mtimes changed, and that full byte restoration cannot be proven. That is honest. But there is no recorded authorized-owner or reviewer acceptance of this incident as non-blocking. The final `assessment.json` should include that disposition, separate from the technical disclosure.
