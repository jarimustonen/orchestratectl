{
  "schema_version": 1,
  "scope": "superseded stripped-PATH setup attempts; none is acceptance evidence",
  "attempts": [
    {
      "symptom": "rustc abort after missing macOS SDK lookup support",
      "cause": "the first declared tool directory omitted the platform xcrun helper",
      "disposition": "superseded compile failure; xcrun was declared in the final tool directory, although rustc still emitted a non-fatal SDK lookup ENOENT before successfully building every test binary"
    },
    {
      "symptom": "cargo-nextest reported unrecognized subcommand run",
      "cause": "direct cargo-nextest invocation omitted its required nextest plugin argv",
      "disposition": "superseded; acceptance invokes cargo-nextest nextest run"
    },
    {
      "symptom": "14 of 1115 tests failed while 1101 passed",
      "cause": "the declared tool directory omitted test fixtures' explicit POSIX true and yes commands; one output-cap test received no yes output and thirteen supervisor tests could not spawn true",
      "disposition": "superseded harness setup; acceptance explicitly declares true and yes"
    },
    {
      "symptom": "thirteen supervisor tests still could not spawn true after it was listed",
      "cause": "command -v returned the shell builtin token true, creating a self-referential symlink instead of linking /usr/bin/true",
      "disposition": "superseded harness setup; acceptance resolves every declared executable with type -P and uses a stable bounded tool directory"
    },
    {
      "symptom": "final exit-0 run retained a non-fatal xcrun SDK lookup ENOENT warning",
      "cause": "the compiler's SDK discovery could not invoke the declared helper inside the stripped environment; exact mechanism was not established",
      "disposition": "accepted disclosed build warning: all release test binaries compiled, all 1115 tests passed, and the full ordinary gate compiled without this stripped-environment condition"
    }
  ],
  "acceptance_log": "recorded by SHA-256 in outputs-summary.txt"
}
