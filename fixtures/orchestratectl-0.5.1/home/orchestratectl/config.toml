[harness]
default = "pi"
[harness.per_kind]
research = "claude"
[profile]
default = "fixture-capable"
[profile.per_kind]
spinoff = "fixture-capable"
[profiles.fixture-capable]
description = "Sanitized fixture profile"
capability = "capable"
residency = "local"
[[profiles.fixture-capable.agents]]
harness = "pi"
command = ["/usr/bin/true", "--fixture"]
telemetry = "worker-v1"
[future-readable]
unknown_key = "preserved"
